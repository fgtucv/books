{
  "name": "books",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "homepage": "https://fgtucv.github.io/books/",
  "scripts": {
    "start": "parcel src/index.html src/basket.html --no-cache",
    "build": "parcel build src/index.html src/basket.html --no-cache --public-url /books/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.13.3"
  }
}
